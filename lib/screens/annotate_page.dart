// lib/screens/annotate_page.dart
import 'package:flutter/material.dart';

class AnnotatePage extends StatelessWidget {
  const AnnotatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Annotate')),
      body: Center(
        // We will build the UI from fig 15 here later.
        child: Text('Annotate Page - Coming Soon!'),
      ),
    );
  }
}
